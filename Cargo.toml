[workspace]
members = [
    "kama-core-traits",
    "kama-buffers",
    "kama-graph",
    "kama-automation",
    "kama-control",
    "kama-signal",
    "kama-registry",
    # "kama-dsp-common",  # временно отключаем
    # "kama-mixer",       # временно отключаем
]
resolver = "2"

[workspace.dependencies]
parking_lot = "0.12"
num-traits = "0.2"
thiserror = "1.0"
async-trait = "0.1"
