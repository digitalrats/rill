[package]
name = "rill-io"
version = "0.3.0"
edition = "2021"
description = "Audio I/O backends for Rill - CPAL, ALSA, PipeWire, JACK"
license.workspace = true
authors.workspace = true
repository.workspace = true


[dependencies]
rill-core = { workspace = true }
rill-graph = { workspace = true, optional = true }
rill-oscillators = { workspace = true, optional = true }
rill-digital-effects = { workspace = true, optional = true }
rill-digital-filters = { workspace = true, optional = true }
thiserror = "1.0"
parking_lot = "0.12"
log = "0.4"
crossbeam-channel = "0.5"

# Бэкенды
cpal = { version = "0.15", optional = true }
alsa = { version = "0.9", optional = true }
pipewire = { version = "0.9", optional = true }
jack = { version = "0.10", optional = true }

# Для конфигурации
serde = { version = "1.0", features = ["derive"], optional = true }

[dev-dependencies]
float-cmp = "0.9"
rill-oscillators = { workspace = true }
rill-digital-filters = { workspace = true }

[features]
default = ["cpal"]

# Бэкенды
cpal = ["dep:cpal"]
alsa = ["dep:alsa"]
pipewire = ["dep:pipewire"]
jack = ["dep:jack"]
all-backends = ["cpal", "alsa", "pipewire", "jack"]

# Интеграция с графом
graph = ["rill-graph"]

# Для примеров
examples = ["rill-oscillators", "rill-digital-effects"]

# Сериализация конфигурации
serde-config = ["dep:serde"]

[[example]]
name = "simple_playback"
path = "examples/simple_playback.rs"
required-features = ["cpal", "alsa", "examples"]

[[example]]
name = "processor_demo"
path = "examples/processor_demo.rs"
required-features = ["cpal", "examples"]

[[example]]
name = "graph_processing"
path = "examples/graph_processing.rs"
required-features = ["graph", "examples"]

[[example]]
name = "granular_processing"
path = "examples/granular_processing.rs"
required-features = ["graph", "examples"]

[[example]]
name = "alsa_demo"
path = "examples/alsa_demo.rs"
required-features = ["alsa", "examples"]

[[example]]
name = "null_demo"
path = "examples/null_demo.rs"
required-features = ["examples"]