[package]
name = "kama-buffers"
version = "0.1.0"
edition = "2021"

[dependencies]
parking_lot = "0.12"
thiserror = "1.0"
rand = "0.8"
kama-core = { path = "../kama-core", version = "0.1.0" }
smallvec = "1.11"

[dev-dependencies]
# Для тестов можно добавить зависимости если нужно